# Hello from Local 👋

This is a demo blog post written in Markdown.

- It’s stored as a file
- The list page pulls metadata from `index.json`
- The single page loads this Markdown and renders it
